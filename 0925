3.9 question
1번
세 가지 명령어 니모닉 예
설명: CPU 기계어 한 동작을 나타내는 기호 이름
ex)mov, add, sub
2번
calling convention이란
함수를 호출할때 인자전달, 레지스터/스택 사용, 반환값 규칙을 정한 약속입니다.
3번
프로그램에서 스택 공간 예약 방법
스택 포인터를 아래로 내리면 그만큼 스택이 생김
명령으로 스택 포인터를 줄여서 공간을 확보한다.
4번
“assembler language”라는 말이 정확하지 않은 이유는?
언어는 사람이 쓰고, assembler는 그걸 기계어로 번역하는 프로그램이기에
실제 언어는 “assembly language”이고, assembler는 그 언어를 번역하는 도구라서 용어가 섞여 있기 때문이다
5번
big endian vs little endian
big endian은 가장 큰 바이트를 낮은 주소에, little endian은 가장 작은 바이트를 낮은 주소에 저장한다.
6번
왜 상수 대신 심볼릭 상수를 쓰는가?
코드에 숫자를 박아 두면 바꾸기 힘들기에 이름 한 군데만 바꾸면 전체 코드에 반영되고, 의미도 더 분명해져서 유지보수가 쉽기 때문이다.
7번
소스 파일과 리스팅 파일 차이
소스 파일은 개발자가 쓴 순수 어셈블리 코드이고, 리스팅 파일은 어셈블러가 주소·기계어·에러 정보 등을 붙여 만든 출력이다.
8번
데이터 레이블과 코드 레이블 차이
데이터 레이블은 변수/데이터 위치를, 코드 레이블은 점프나 호출 대상이 되는 명령어 위치를 가리킨다.
9번
식별자는 숫자로 시작할 수 없다.
이것은 대부분의 어셈블러 규칙에 해당하기에 정답이다 
정답: True
10번
16진 리터럴은 0x3A처럼 쓸 수 있다.
C 계열과 동일한 표기이기에 정답이다.
정답: True
11번
어셈블리 지시어(directive)는 실행 시간에 수행된다.
지시어는 어셈블러에게만 영향을 주는 것이기에 어셈블 단계에서만 처리한다. 그래서 답이 아니다.
정답: False
12번
어셈블리 지시어는 대소문자를 섞어서 써도 된다.
대부분의 어셈블러에서 지시어 이름은 대소문자를 구문하지 않기에 정답이 아니다.
정답: False
13번
어셈블리 명령어의 네 가지 기본 부분
답: 1, 레이블 2, 니모닉 3, 피연산자들 4, 주석
14번
MOV는 명령어 니모닉의 예이다.
MOV는 데이터 이동을 뜻하는 명령 이름이고 위에 설명은 참이다.
정답: True
15번
코드 레이블 뒤에는 콜론이 오지만, 데이터 레이블 뒤에는 콜론이 오지 않는다.
MASM 스타일 기준, 대게 둘 다 콜론을 사용하거나, 어셈블러 규칙에 따라 동일하게 처리하기에 정답이 아니다.
정답: False
16번
블록 주석의 예시 적기
COMMENT !
여기부터
여러 줄 주석
!
17번
변수를 접근할 때 숫자 주소를 직접 쓰면 안 좋은 이유
코드가 다른 환경이나 재배치에 약하고, 의미를 알기 어렵고, 주소가 바뀌면 전부 다시 고쳐야 해서 유지보수가 힘들기 때문이다.
18번
ExitProcess에 어떤 타입의 인자를 넘겨야 하는가?
프로세스 종료 코드를 나타내는 값 하나를 넘긴다. 종료코드, 보통 unsigned int
32비트 정수
19번
어떤 지시어가 하나의 프로시저를 끝내는가?
PROC와 쌍을 이루는 종료용 지시어.
ENDP
20번
32비트 모드에서 END 지시어에 붙는 식별자의 용도는?
어떤 레이블에서 실행을 시작할지(엔트리 포인트)를 지정한다.
21번
PROTO 지시어의 목적은?
C의 함수 프로토타입처럼, 나중에 정의될 프로시저의 인터페이스를 미리 선언하는 역할이다. 간단하게 말하면
프로시저 이름과 인자 목록을 선언해서 다른 곳에서 호출할 수 있게 한다.
22번
오브젝트 파일은 링커가 만든다.
오브젝트(.obj)는 어셈블러/컴파일러 출력이고, 링커 출력은 실행 파일이다. 그래서 정답이 아니다.
정답: False
23번
리스팅 파일은 어셈블러가 만든다.
주소, 기계어, 소스 코드가 함께 들어 있는 보고서 파일을 말하는 것이 맞기에 참이다.
정답: True
24번
실행 파일을 만들기 직전에 링크 라이브러리가 프로그램에 추가된다.
링커가 여러 오브젝트와 라이브러리를 묶어 하나의 실행 파일을 만들고 위에서 말하는 것처럼 직전에 만들기에 참이다.
정답: True
25번
32비트 부호 있는 정수 변수를 만드는 데이터 지시어
부호 여부는 해석 차이이고, 크기만 맞으면 된다.
정답: SDWORD
26번
16비트 부호 있는 정수 변수를 만드는 데이터 지시어
정답: SWORD
27번
64비트 부호 없는 정수 변수를 만드는 데이터 지시어
정답: QWORD
28번
8비트 부호 있는 정수 변수를 만드는 데이터 지시어
정답: SBYTE
29번
10바이트 packed BCD 변수를 만드는 데이터 지시어
TBYTE


